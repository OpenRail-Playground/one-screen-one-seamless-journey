import { CarItemProperties } from './car-item.properties';
import { CarProperties } from '../car/car.properties';
export declare const CarItemHtml: ({ active, vehicle, variant, anchor, transportType, interactive, carIndex, postfix }: CarItemProperties & CarProperties) => import("lit-html").TemplateResult<1>;
