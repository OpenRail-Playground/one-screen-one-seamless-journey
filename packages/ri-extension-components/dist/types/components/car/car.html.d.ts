import { CarProperties } from './car.properties';
export declare const CarHtml: ({ vehicle, active, variant, anchor, interactive, transportType, riClick, carIndex, postfix }: CarProperties) => import("lit-html").TemplateResult<1>;
