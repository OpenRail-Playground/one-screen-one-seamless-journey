/**
 * Type declaration for @db-ux-inner-source/ri-extension-components.
 * Registers the web components (ri-board, ri-board-item, ri-stop, etc.) when imported.
 */
declare module '@db-ux-inner-source/ri-extension-components' {
  // Side-effect import that registers all RI web components
}
